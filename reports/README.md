# `reports/`

- `weekly/<snapshot_week>.md` — weekly narrative snapshot (one file per reporting window; generated by the pipeline).
- `cards/<snapshot_week>.png` — static share card image for Slack / LinkedIn / X.

Both are generated by Bruin assets, not hand-written. Old files are kept so social posts retain working permalinks.
